# KOMENTARZ Language with type fraction
# eg. 4%7

void printFraction(string s, fraction f){
	#print(s, f);
}


class Klasa{

void funkcja(fraction fra){
	fraction a  = fra;
}

fraction var;
string str = "cos";

} 

void main(){
fraction contener[] = {4%9, 6%754, 2%9, -3%4,7%2};
fraction sum = 0;

for(fraction i : contener){ 
	sum = sum + i;
}

fraction a = 1%4;
while(a > 1%16){
a = a*a;
}
printFraction("Value of a: ", a );

fraction b = 10;
while(b<12) {
	if ((b / 5 + 9%3) > 4) {
		b=b-3;
	} else {
		b = 14;
	}
}

printFraction("Value of b: ", b);

return 0;
}